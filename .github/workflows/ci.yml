name: Mobile CI

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

jobs:
  mobile-quality:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22.13.0"

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Install dependencies
        run: npm install

      - name: Verify Expo dependency compatibility
        run: npx expo install --check

      - name: Lint
        run: npm run lint

      - name: Type-check
        run: npm run typecheck

      - name: Run tests
        run: npm test

      - name: Run mobile release audit
        run: npm run audit:release

      - name: Export Android bundle
        run: npx expo export --platform android --output-dir dist/android

      - name: Build standalone Android release APK
        run: npm run build:android:private

      - name: Verify embedded React Native bundle
        run: |
          unzip -l android/app/build/outputs/apk/release/app-release.apk | grep -q "assets/index.android.bundle"

      - name: Verify APK signature
        run: |
          APKSIGNER=$(find "$ANDROID_HOME/build-tools" -type f -name apksigner | sort -V | tail -n 1)
          test -n "$APKSIGNER"
          "$APKSIGNER" verify --verbose android/app/build/outputs/apk/release/app-release.apk

      - name: Read app version
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        id: app-version
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Stage versioned Android APK
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          mkdir -p release
          cp android/app/build/outputs/apk/release/app-release.apk "release/Smoothie-Generator-v${{ steps.app-version.outputs.version }}.apk"

      - name: Upload private Android APK artifact
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: actions/upload-artifact@v4
        with:
          name: smoothie-generator-android-v${{ steps.app-version.outputs.version }}
          path: release/Smoothie-Generator-v${{ steps.app-version.outputs.version }}.apk
          if-no-files-found: error
          retention-days: 30
